import 'package:flutter/cupertino.dart';

class ReportOutgoingPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        'Report Incoming Page',
        style: TextStyle(fontSize: 24),
      ),
    );
  }
}

