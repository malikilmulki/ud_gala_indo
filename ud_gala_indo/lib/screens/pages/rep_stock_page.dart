import 'package:flutter/cupertino.dart';

class ReportStockPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        'Report Incoming Page',
        style: TextStyle(fontSize: 24),
      ),
    );
  }
}

